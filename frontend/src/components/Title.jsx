const Title = ({ title }) => {
  return (
    <div>
      <h1 className="uppercase font-bold mb-5 text-center text-3xl text-accent ">
        {title}
      </h1>
    </div>
  );
};

export default Title;